spring:
    application:
        name: eureka-server

server:
    port: 8761

eureka:
    client: 
        register-with-eureka: false # Le dice que no se registre en si mismo
        fetch-registry: false # Le dice al servidro eureka que no busque otras rutas de otros
                              # servidores eureka porque es el único que tenemos





